import * as semanticConventions from '../src';
import * as assert from 'assert';

describe('semantic-conventions', () => {
  it('asserts', () => {
    assert(typeof semanticConventions === 'object');
  });
});
